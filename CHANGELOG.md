# 变更记录

## 0.4.1

- 重设计侧边栏：偏好与用量分离，高级参数、连接配置及诊断默认折叠。
- 新增统一的扩展 PNG 图标、品牌 SVG 与活动栏单色图标。
- 预设显示选中状态，修改后才显示保存操作，保留保存期间的新编辑。
- README 精简为插件介绍、使用方法和配置参数说明。

## 0.4.0

- 官方 DeepSeek 实现类注释直接路由至非思考 Chat 补全，普通续写保留 FIM。
- 传递生成行数预算，提示模型优先完成最小输入/输出逻辑，避免无关脚手架。
- 增加重复代码块、混合 Markdown、批量无关元数据过滤与拦截计数。
- 新增 Harness 真实 JSON/SSE 生成、语法及两组输入输出验证；不再将非空候选当成需求实现证据。

## 0.3.3

- 将多行意图从少量函数声明关键字扩展为跨语言紧邻注释识别，不要求 TODO 字样。
- 支持行注释、文档/块注释、HTML、SQL、Lua、PowerShell、Lisp 等语法及未知语言回退。
- FIM 关联片段使用目标语言适用的注释包装，原始前后文保持不变。
- 增加 22 组跨语言 JSON/SSE 管线回归和多语言真实编辑器接受测试。

## 0.3.2

- 缓存优先于防抖，检索与等待重叠，防抖/节流截止时间合并。
- 默认输入等待降至 150ms、服务请求间隔降至 250ms，保留已有用户设置。
- 空候选短暂缓存，减少同一位置反复请求；保留取消、冷却与用量记账。
- 新增极速/均衡/完整表单预设、候选来源和分阶段耗时显示。
- 新增调度回归测试和可复现本地模拟基准。

## 0.3.1

- 修复 Python `def ` 等未完成声明在自动模式下被截为单行，验证中文 TODO 上下文与多行接受。
- 新增 JSON/SSE usage 回调及本地持久化累计 token，缓存不重复计数，未知用量明确区分。
- 新增 DeepSeek 官方账户余额查询、侧边栏入口及可选 Provider 余额接口。
- 保持 v1 Provider 兼容，新增协议、记账和真实 VS Code Python 场景测试。

## 0.3.0

- 接入 GhostTextAcceptanceTracker：文档事务和光标事件优先识别接受，支持部分接受、最小化替换与 CRLF。
- 新增 Worker 项目索引，未打开的 TS/JS 导出函数可参与本地 IntelliSense、导入建议和模型上下文。
- 新增基于调用绑定的签名联动与同作用域相邻重复编辑，CodeLens 差异预览后逐项接受。
- 应用前验证快照，应用后保留并重定位剩余建议，防止补丁回声学习。
- 修复 Windows 工作区 URI 大小写导致候选丢失，以及资源设置作用域日志问题。
- 新增项目场景宿主测试、真实 DeepSeek 项目函数调用验证和本地索引性能脚本。

## 0.2.0

- 新增中文侧边栏、配置表单、运行统计、状态栏快捷操作与暂停功能。
- 重构为配置、密钥、Provider、上下文、引擎、编辑器和 UI 模块，关键逻辑使用中文注释。
- 支持 FIM/Chat SSE 流式响应、长度控制、限流冷却、候选复用和保守后处理。
- 可选最近文件与语言服务定义上下文，加入统一排除规则、ignore 文件和文件大小限制。
- 支持原生 IntelliSense 联动、多光标过滤、Notebook 单元格和接受统计。
- 扩充自动测试，增加真实 VS Code 宿主测试、在线 DeepSeek 验证和独立侧边栏预览。
- 保持原密钥标识及 v1 Provider API 兼容。

## 0.1.0

- 基础 DeepSeek FIM 行内补全、API Key 安全存储及自定义 Provider。
